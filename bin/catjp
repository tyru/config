#!/bin/sh


if [ $(uname -o) = "Cygwin" ]; then
    encoding="windows"
else
    encoding="unix"
fi

while getopts e: opt; do
    case $opt in
        "e" ) encoding="$OPTARG" ;;
        *   ) usage ; exit 0 ;;
    esac
done

cat $* | nkf --$encoding
