#!/usr/bin/env perl
use strict;
use warnings;
use utf8;

my $pid = fork;
unless (defined $pid) {
    die "Can't fork: $!";
}

if ($pid) {
    wait;
} else {
    exec("/bin/ls", '/tmp');
    exit 1;
}
