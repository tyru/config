#include "prime3.h"
#include <cstdlib>


int
main(int argc, char *argv[]) {

    const int MAX_COUNT = 100 * 1000;


    for (int i = 1; i < MAX_COUNT; ++i) {
        isPrime(i);
    }

    return EXIT_SUCCESS;
}
