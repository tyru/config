#!/bin/sh

PROG="prime prime2 prime3"


echo -n "compiling..."
for i in "$PROG"; do
    g++ ${i}.cpp -o ${i}
    ./$i > $i.txt
done
echo "Done."

echo "keisokutyu..."
for i in "$PROG"; do
    sleep 1
    time ./$i > /dev/null
done
