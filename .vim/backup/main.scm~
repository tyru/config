; ------------------------------------------------
(use srfi-1)
(use gauche.collection)

(define back #f)
(define a #f)

;; main
(define (main args)

  (call/cc
    (lambda (cont)
      (set! back cont)
      (print "a:" a)
      (set! a #t)
      ))
  (print "a:" a)
  (back)


  0)
