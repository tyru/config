#!/usr/bin/env perl
use strict;
use warnings;
use utf8;

use Text::Wrap qw(wrap);
use File::Basename;
use File::Spec;


my $dir = shift || '.';
my @files = glob File::Spec->catfile($dir, '*');
if ($dir eq '.') {
    $_ = basename($_) for (@files);
}
print wrap('', '', join "  ", @files) . "\n";
