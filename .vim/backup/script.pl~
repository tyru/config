#!/usr/bin/env perl
use strict;
use warnings;
use utf8;

use Getopt::Long qw(:config auto_help gnu_compat);
use Perl6::Say;
use Pod::Usage;

use FindBin;
use lib "$FindBin::Bin/lib";

use Text::Tokenizer::Class;

my $parser = Text::Tokenizer::Class->new(parser_type => 'handler');





__END__

(print (+ 1 1))    # => 2
(print (+ (* 3 3) (* 4 4)))    # => 25
