class Human(no:Int) extends scala.actors.Actor {
    def act() = println(no + "人目 誕生")
}

object Main {
    def main(args:Array[String]) = {
        // 子孫繁栄
        (1 to 6).foreach(no => {
            println(no + "人目 着床")
            new Human(no) start
        })
    }
}

// Threadとどう違うのよ
