#!/usr/bin/env perl
use strict;
use warnings;
use utf8;

use IO::String;

my $str = IO::String->new(<<EOF);
Hello, World!
This is an example of use of IO::String.
EOF

until ($str->eof) {
    print $str->getline;
}

print "isa:" . $str->isa('IO::String');
